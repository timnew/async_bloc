name: async_bloc
description: A bloc that accepts async source as input
version: 0.0.1
author: timnew
homepage: https://github.com/timnew/async_bloc

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 7.0.0-nullsafety.5

dev_dependencies:
  pedantic: ^1.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.5
  mockito: ^4.1.3

flutter:
